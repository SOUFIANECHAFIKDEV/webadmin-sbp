export enum gammeElements {
    lot = 1,
    operation = 2
}

export enum Crud_gamme_maintenance_equipement_Enum {
    ajouterLot = 1,
    modifierLot = 2,
    supprimerLot = 3,
    ajouterOperation = 4,
    modifierOperation = 5,
    supprimerOperation = 6,
}